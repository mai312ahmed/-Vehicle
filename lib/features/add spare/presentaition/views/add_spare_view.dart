import 'package:flutter/material.dart';

import 'widgets/add_spare_view_body.dart';

class AddSpareView extends StatelessWidget {
  const AddSpareView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: AddSpareViewBody(),
    );
  }
}
