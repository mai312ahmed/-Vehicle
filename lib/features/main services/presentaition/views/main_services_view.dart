import 'package:flutter/material.dart';

import 'widgets/main_services_view_body.dart';

class MainServicesView extends StatelessWidget {
  const MainServicesView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: MainServicesViewBody(),
    );
  }
}
