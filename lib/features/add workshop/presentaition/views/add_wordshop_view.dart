import 'package:flutter/material.dart';

import 'widgets/add_wordshop_view_body.dart';

class AddWordshopView extends StatelessWidget {
  const AddWordshopView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: AddWordshopViewBody(),
    );
  }
}
