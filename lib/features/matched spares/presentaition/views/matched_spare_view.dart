import 'package:flutter/material.dart';

import 'widgets/matched_spare_view_body.dart';

class MatchedSpareView extends StatelessWidget {
  const MatchedSpareView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: MatchedSpareViewBody(),
    );
  }
}
